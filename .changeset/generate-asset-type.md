---
"minimizer-webpack-plugin": minor
---

Allow a named generator to be an object with `type: "asset"`, `filename`, `filter` and `deleteOriginalAssets`, which generates a new asset beside an emitted one.
