---
"minimizer-webpack-plugin": minor
---

add the `napiRsImageMinify` image minimizer, using `@napi-rs/image` codecs — oxipng for lossless `png`, mozjpeg for `jpeg`, and its own encoders for `avif` and `webp`
